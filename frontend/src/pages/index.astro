---
import type { PageType } from "../static/types/types"
import Template from "../layouts/Template.astro"
import About from "../layouts/About.astro"
import AboutThumbnails from "../layouts/AboutThumbnails.astro"
import AboutHero from "../components/aboutSection/hero/AboutHero.astro"
import Footer from "../components/aboutSection/footer/Footer.astro"
import AnimateOnScroll from "../components/_scripts/AnimateOnScroll.astro"

const activeTab: PageType["active"] = "about"
---

<Template active={activeTab}>
    <!-- Background Toggle Button -->
    <div class="toggle-background" id="backgroundToggle">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2v2"></path>
            <path d="M12 20v2"></path>
            <path d="M5 5l1.5 1.5"></path>
            <path d="M17.5 17.5L19 19"></path>
            <path d="M2 12h2"></path>
            <path d="M20 12h2"></path>
            <path d="M5 19l1.5-1.5"></path>
            <path d="M17.5 6.5L19 5"></path>
            <circle cx="12" cy="12" r="5"></circle>
        </svg>
    </div>

    <AboutHero />
    <section id="details" class="relative left-1/2 -translate-x-1/2 w-[100dvw] max-w-[1250px] grid grid-cols-1 md:grid-cols-[250px_1fr] gap-x-[100px]">
        <AboutThumbnails />
        <About />
        <Footer />
    </section>
</Template>

<!-- scripts ----------------------->
<AnimateOnScroll />

<!-- Background Toggle Script -->
<!-- <script>
    document.addEventListener('DOMContentLoaded', function() {
        const toggleButton = document.getElementById('backgroundToggle');
        const bodyElement = document.body;

        if (toggleButton) {
            toggleButton.addEventListener('click', function() {
                const currentBackgroundColor = window.getComputedStyle(bodyElement).backgroundColor;

                if (currentBackgroundColor === 'rgb(0, 0, 0)') { // Black background
                    bodyElement.style.backgroundColor = 'white';
                    bodyElement.style.color = 'black';
                } else { // White background
                    bodyElement.style.backgroundColor = 'black';
                    bodyElement.style.color = 'white';
                }
            });
        }
    });
</script> -->

<style is:global>
    .slide-up { animation: slideUp .75s forwards; }
    .slide-up-hero { animation: slideUpHero .75s forwards; }

    .active-sidebar { 
        position: relative;
        color: #fff;
    }
    .active-sidebar::before {
        content: '•';
        color: "#fff";
        position: absolute;
        top: 50%;
        left: -8px;
        transform: translateY(-50%);
    }

    @keyframes slideUp {
        from { transform: translateY(70px);  }
        to   { transform: translateY(0px); }
    }
    
    @keyframes slideUpHero {
        from { transform: translate(-50%,70px);  }
        to   { transform: translate(-50%,0px); }
    }

    /* Global CSS for Background Toggle Button */
    .toggle-background {
        position: fixed;
        bottom: 20px;
        right: 20px;
        background-color: rgba(0, 0, 0, 0.5);
        border-radius: 50%;
        padding: 10px;
        cursor: pointer;
        /* z-index: 9999; Ensure it's on top */
        transition: background-color 0.3s ease;
    }

    .toggle-background:hover {
        background-color: rgba(255, 255, 255, 0.2);
    }

    .toggle-background svg {
        fill: none;
        stroke: #fff;
        stroke-width: 2px;
    }

    /* Set initial background and text colors */
    body {
        background-color: black;
        color: white;
        font-family: Arial, sans-serif; /* Optional: Set a global font */
        margin: 0;
        padding: 0;
        transition: background-color 0.3s ease, color 0.3s ease;
    }
</style>